# exact versions only; package-lock.json is the source of truth and CI uses npm ci
save-exact=true
engine-strict=true

# tolerate a slow registry connection
fetch-timeout=300000
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
